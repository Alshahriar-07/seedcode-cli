# Runtime dependencies (mirrors pyproject.toml [project.dependencies]).
openai>=1.40.0
rich>=13.7.0
prompt_toolkit>=3.0.43
pydantic>=2.6.0
httpx>=0.27.0

# Computer Engine + detection ladder (pyproject [project.optional-dependencies]
# desktop). Listed here so a plain `pip install -r requirements.txt` gets a
# machine that can actually run desktop and browser automation, including the
# OCR/CV tiers — the user is never asked to install these separately.
# Windows-only markers keep the file installable on other platforms.
pyautogui>=0.9.54; sys_platform == 'win32'
mss>=9.0.1; sys_platform == 'win32'
pygetwindow>=0.0.9; sys_platform == 'win32'
uiautomation>=2.0.18; sys_platform == 'win32'
pillow>=10.0.0; sys_platform == 'win32'
psutil>=5.9.0; sys_platform == 'win32'
pytesseract>=0.3.10; sys_platform == 'win32'
opencv-python>=4.9.0; sys_platform == 'win32'
